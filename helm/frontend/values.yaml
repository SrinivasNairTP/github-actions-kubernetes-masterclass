replicaCount: 1

image:
  repository: srini2390/skillpulse-frontend
  tag: latest
  pullPolicy: Always

service:
  type: LoadBalancer
  port: 80
  targetPort: 8080

resources:
  requests:
    cpu: 50m
    memory: 32Mi

  limits:
    memory: 64Mi